# Metafoks MongoDB Extension

## How to use?

```shell
npm i @metafoks/mongodb
```

setup `config/config.json` file:

```json
{
  "mongodb": {
    "database": "databaseName",
    "uri": "uri string"
  }
}
```
